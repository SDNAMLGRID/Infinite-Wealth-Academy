---
import GlobalLayout from '../../layouts/GlobalLayout.astro';
---

<GlobalLayout
  title="Ressources interactives | Infinite Wealth Academy"
  description="Utilisez notre calculateur d’écart de revenu à la retraite et l’évaluation de clarté financière. Résultats instantanés et pédagogiques."
>
  <!-- Hero -->
  <section class="py-20 border-b border-white/10" style="background-color: #1A2C46;">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="text-4xl md:text-5xl font-extrabold tracking-tight mb-6" style="color: #FFFFFF;">
        Ressources <span style="color: #CCA03F;">interactives</span>
      </h1>
      <p class="text-xl max-w-2xl mx-auto leading-relaxed" style="color: #E0E4EB;">
        Obtenez une clarté immédiate sur votre avenir financier. Identifiez vos angles morts et découvrez pourquoi l’épargne seule n’est pas suffisante.
      </p>
    </div>
  </section>

  <!-- Tool 1 -->
  <section class="py-20 bg-slate-50">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-extrabold text-slate-900">1. Calculateur d’écart de revenu à la retraite</h2>
        <p class="mt-4 text-lg text-slate-600">Estimez votre revenu à la retraite et découvrez un éventuel écart caché.</p>
      </div>

      <div class="bg-white rounded-2xl shadow-xl overflow-hidden flex flex-col lg:flex-row mb-12" style="border-top: 4px solid #CCA03F;">
        <div class="p-10 lg:w-5/12 bg-white border-r border-slate-100">
          <form id="calculator-form" class="space-y-5">
            <div>
              <label class="block text-sm font-bold text-slate-700 mb-1">Âge actuel</label>
              <input type="number" id="currentAge" value="35" min="18" max="100" class="w-full px-4 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 text-slate-900 font-semibold focus:outline-none" />
            </div>
            <div>
              <label class="block text-sm font-bold text-slate-700 mb-1">Âge de retraite souhaité</label>
              <input type="number" id="retireAge" value="65" min="18" max="100" class="w-full px-4 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 text-slate-900 font-semibold focus:outline-none" />
            </div>
            <div>
              <label class="block text-sm font-bold text-slate-700 mb-1">Épargne retraite actuelle ($)</label>
              <input type="number" id="currentSavings" value="50000" min="0" step="1000" class="w-full px-4 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 text-slate-900 font-semibold focus:outline-none" />
            </div>
            <div>
              <label class="block text-sm font-bold text-slate-700 mb-1">Contribution mensuelle ($)</label>
              <input type="number" id="monthlyContribution" value="500" min="0" step="50" class="w-full px-4 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 text-slate-900 font-semibold focus:outline-none" />
            </div>
            <div>
              <label class="block text-sm font-bold text-slate-700 mb-1">Revenu annuel souhaité à la retraite ($)</label>
              <input type="number" id="desiredIncome" value="80000" min="0" step="5000" class="w-full px-4 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 text-slate-900 font-semibold focus:outline-none" />
            </div>
          </form>
        </div>

        <div class="p-10 lg:w-7/12 flex flex-col justify-center border-t lg:border-t-0 lg:border-l border-white/10" style="background-color: #1A2C46;">
          <h3 class="text-xl font-bold mb-6 border-b border-white/10 pb-4" style="color: #CCA03F;">Vos projections</h3>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
            <div>
              <p class="text-slate-400 text-sm uppercase tracking-wide">Épargne totale estimée</p>
              <p class="text-3xl font-extrabold" style="color: #FFFFFF;" id="resultTotalSavings">$0</p>
              <p class="text-xs text-slate-500 mt-1">*Hypothèse : 6 % de croissance annuelle</p>
            </div>
            <div>
              <p class="text-slate-400 text-sm uppercase tracking-wide">Revenu annuel « prudent »</p>
              <p class="text-3xl font-extrabold" style="color: #FFFFFF;" id="resultAnnualIncome">$0</p>
              <p class="text-xs text-slate-500 mt-1">*Basé sur un retrait de 4 %</p>
            </div>
          </div>

          <div class="bg-white/5 p-5 rounded-xl mb-8 flex justify-between items-center border-l-4 border-red-500" id="gap-container">
            <div>
              <p class="text-slate-300 text-sm font-bold uppercase tracking-wide">Votre écart de revenu</p>
              <p class="text-3xl font-bold text-red-400" id="resultGap">$0</p>
            </div>
          </div>

          <div class="p-6 rounded-xl border shadow-inner bg-white/5" style="border-color: rgba(204, 160, 63, 0.3);">
            <h4 class="font-bold mb-2 flex items-center" style="color: #CCA03F;">
              <svg class="w-5 h-5 mr-2 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path></svg>
              Insight conseiller : le danger de « juste épargner »
            </h4>
            <p class="text-sm text-slate-300 leading-relaxed mb-4">
              Les conseils traditionnels négligent souvent la protection et la flexibilité. L’écart ci-dessus apparaît parce qu’il existe une grande différence entre <strong>épargner</strong> et <strong>positionner</strong> vos actifs pour générer un revenu fiable, fiscalement avantageux et résistant à l’inflation.
            </p>
            <a href="/fr/book" class="inline-block text-sm font-bold transition-colors hover:opacity-90" style="color: #CCA03F;">
              Fixons cet écart ensemble &rarr;
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Tool 2 -->
  <section class="py-20 bg-white border-t border-slate-200">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-extrabold text-slate-900">2. Évaluation de clarté financière</h2>
        <p class="mt-4 text-lg text-slate-600">La protection financière peut être simple. Répondez à ces 4 questions pour révéler vos angles morts.</p>
      </div>

      <div class="bg-slate-50 rounded-2xl shadow-lg border border-slate-200 overflow-hidden flex flex-col lg:flex-row">
        <div class="p-10 lg:w-2/3 bg-white">
          <form id="clarity-form" class="space-y-8">
            <div class="border-b border-slate-100 pb-6">
              <div class="flex items-start justify-between gap-4 mb-3">
                <div class="flex-1 pr-4">
                  <h4 class="font-bold text-slate-900 text-lg">1. Protection du revenu</h4>
                  <p class="text-slate-700 mt-1">Si vous ou votre conjoint ne pouviez plus travailler (maladie, accident), votre revenu est-il protégé sans épuiser votre épargne ?</p>
                </div>
                <select class="clarity-input flex-shrink-0 w-24 px-3 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 font-bold text-slate-900">
                  <option value="0">Non</option>
                  <option value="25">Oui</option>
                </select>
              </div>
              <div class="p-4 rounded-lg border-l-2 text-sm text-slate-600 italic bg-slate-50" style="border-left-color: #CCA03F;">
                <strong>Pourquoi c’est important :</strong> Beaucoup assurent leur maison et leur voiture, mais oublient leur actif le plus précieux : la capacité à générer un revenu. La clarté exige un filet de sécurité stratégique.
              </div>
            </div>

            <div class="border-b border-slate-100 pb-6">
              <div class="flex items-start justify-between gap-4 mb-3">
                <div class="flex-1 pr-4">
                  <h4 class="font-bold text-slate-900 text-lg">2. Prestations du vivant</h4>
                  <p class="text-slate-700 mt-1">Avez-vous une assurance qui permet d’accéder à des fonds de votre vivant pour couvrir une urgence médicale ou de vie ?</p>
                </div>
                <select class="clarity-input flex-shrink-0 w-24 px-3 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 font-bold text-slate-900">
                  <option value="0">Non</option>
                  <option value="25">Oui</option>
                </select>
              </div>
              <div class="p-4 rounded-lg border-l-2 text-sm text-slate-600 italic bg-slate-50" style="border-left-color: #CCA03F;">
                <strong>Pourquoi c’est important :</strong> L’assurance vie traditionnelle paie souvent au décès. Les stratégies modernes utilisent des « prestations du vivant » : vous n’avez pas à mourir pour bénéficier d’un soutien en cas d’événement de santé majeur.
              </div>
            </div>

            <div class="border-b border-slate-100 pb-6">
              <div class="flex items-start justify-between gap-4 mb-3">
                <div class="flex-1 pr-4">
                  <h4 class="font-bold text-slate-900 text-lg">3. Stratégies fiscalement avantageuses</h4>
                  <p class="text-slate-700 mt-1">Vos actifs retraite sont-ils positionnés pour réduire l’impôt lors des retraits ?</p>
                </div>
                <select class="clarity-input flex-shrink-0 w-24 px-3 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 font-bold text-slate-900">
                  <option value="0">Non</option>
                  <option value="25">Oui</option>
                </select>
              </div>
              <div class="p-4 rounded-lg border-l-2 text-sm text-slate-600 italic bg-slate-50" style="border-left-color: #CCA03F;">
                <strong>Pourquoi c’est important :</strong> Ce n’est pas seulement ce que vous gagnez, c’est ce que vous gardez. Sans positionnement fiscal, l’impôt peut devenir votre plus grande dépense à la retraite.
              </div>
            </div>

            <div>
              <div class="flex items-start justify-between gap-4 mb-3">
                <div class="flex-1 pr-4">
                  <h4 class="font-bold text-slate-900 text-lg">4. Flexibilité du revenu à la retraite</h4>
                  <p class="text-slate-700 mt-1">Avez-vous une visibilité et un contrôle sur vos anciens comptes retraite (ex. anciens 401(k)) ?</p>
                </div>
                <select class="clarity-input flex-shrink-0 w-24 px-3 py-2 border border-slate-300 rounded-md focus:ring-2 focus:ring-[#CCA03F] focus:border-[#CCA03F] bg-slate-50 font-bold text-slate-900">
                  <option value="0">Non</option>
                  <option value="25">Oui</option>
                </select>
              </div>
              <div class="p-4 rounded-lg border-l-2 text-sm text-slate-600 italic bg-slate-50" style="border-left-color: #CCA03F;">
                <strong>Pourquoi c’est important :</strong> Laisser des comptes derrière soi après un changement d’emploi est un risque majeur. Ils manquent souvent de flexibilité pour s’adapter à la vie et à la volatilité des marchés.
              </div>
            </div>
          </form>
        </div>

        <div class="p-10 lg:w-1/3 flex flex-col items-center justify-center text-center border-t lg:border-t-0 lg:border-l border-white/10" style="background-color: #1A2C46;">
          <h3 class="text-xl font-bold mb-4 uppercase tracking-wide text-slate-300">Votre score de clarté</h3>
          <div class="relative w-40 h-40 flex items-center justify-center rounded-full border-8 mb-6 shadow-lg transition-colors duration-500 bg-white/5" id="score-circle" style="border-color: #ef4444;">
            <span class="text-5xl font-extrabold" style="color: #FFFFFF;" id="clarity-score">0%</span>
          </div>
          <p class="text-slate-300 text-sm mb-8 min-h-[60px] leading-relaxed" id="clarity-message">
            Vous avez des angles morts financiers importants. Des risques peuvent menacer votre patrimoine.
          </p>
          <a href="/fr/book" class="w-full px-6 py-4 text-base font-bold rounded-md transition-all shadow-lg hover:brightness-110 hover:scale-[1.02]" style="background-color: #CCA03F; color: #FFFFFF;">Corrigeons vos angles morts</a>
        </div>
      </div>
    </div>
  </section>

  <script>
    function calculateRetirement() {
      const currentAge = parseInt(document.getElementById('currentAge').value, 10) || 0;
      const retireAge = parseInt(document.getElementById('retireAge').value, 10) || 0;
      const currentSavings = parseFloat(document.getElementById('currentSavings').value) || 0;
      const monthlyContribution = parseFloat(document.getElementById('monthlyContribution').value) || 0;
      const desiredIncome = parseFloat(document.getElementById('desiredIncome').value) || 0;

      const yearsToGrow = Math.max(0, retireAge - currentAge);
      const annualRate = 0.06;
      const monthlyRate = annualRate / 12;
      const totalMonths = yearsToGrow * 12;

      const futureSavings = currentSavings * Math.pow(1 + annualRate, yearsToGrow);
      let futureContributions = 0;
      if (monthlyRate > 0) {
        futureContributions = monthlyContribution * ((Math.pow(1 + monthlyRate, totalMonths) - 1) / monthlyRate);
      }

      const totalFutureValue = futureSavings + futureContributions;
      const expectedAnnualIncome = totalFutureValue * 0.04;
      const incomeGap = Math.max(0, desiredIncome - expectedAnnualIncome);

      const formatter = new Intl.NumberFormat('fr-CA', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 });

      document.getElementById('resultTotalSavings').textContent = formatter.format(totalFutureValue);
      document.getElementById('resultAnnualIncome').textContent = formatter.format(expectedAnnualIncome);

      const gapEl = document.getElementById('resultGap');
      const gapContainer = document.getElementById('gap-container');

      if (incomeGap > 0) {
        gapEl.textContent = formatter.format(incomeGap) + ' / an';
        gapEl.className = 'text-3xl font-bold text-red-400';
        gapContainer.className = 'bg-white/5 p-5 rounded-xl mb-8 flex justify-between items-center border-l-4 border-red-500';
      } else {
        gapEl.textContent = 'Objectif atteint !';
        gapEl.className = 'text-3xl font-bold text-green-400';
        gapContainer.className = 'bg-white/5 p-5 rounded-xl mb-8 flex justify-between items-center border-l-4 border-green-500';
      }
    }

    document.querySelectorAll('#calculator-form input').forEach(function (input) {
      input.addEventListener('input', calculateRetirement);
    });
    calculateRetirement();

    function calculateClarity() {
      const inputs = document.querySelectorAll('.clarity-input');
      let totalScore = 0;
      inputs.forEach(function (input) {
        totalScore += parseInt(input.value, 10);
      });

      const scoreDisplay = document.getElementById('clarity-score');
      const scoreCircle = document.getElementById('score-circle');
      const messageDisplay = document.getElementById('clarity-message');

      scoreDisplay.textContent = totalScore + '%';

      if (totalScore === 100) {
        scoreCircle.style.borderColor = '#22c55e';
        messageDisplay.textContent = 'Excellent ! Vous avez une grande clarté financière. Optimisons votre plan.';
      } else if (totalScore >= 50) {
        scoreCircle.style.borderColor = '#eab308';
        messageDisplay.textContent = 'Vous avez une base solide, mais des protections clés peuvent manquer. Renforçons votre flexibilité.';
      } else {
        scoreCircle.style.borderColor = '#ef4444';
        messageDisplay.textContent = 'Vos angles morts sont importants. Planifions une analyse des besoins financiers pour sécuriser vos objectifs.';
      }
    }

    document.querySelectorAll('.clarity-input').forEach(function (input) {
      input.addEventListener('change', calculateClarity);
    });
    calculateClarity();
  </script>
</GlobalLayout>
