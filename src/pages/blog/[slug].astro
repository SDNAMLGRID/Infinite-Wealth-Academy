---
import GlobalLayout from '../../layouts/GlobalLayout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const blogEntries = await getCollection('blog');
  return blogEntries.map((entry) => ({
    params: { slug: entry.id },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---

<GlobalLayout title={`${entry.data.title} | Infinite Wealth Academy`} description={entry.data.description}>
  <article class="bg-white pt-20 pb-24">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <header class="text-center mb-12 border-b border-slate-200 pb-10">
        <p class="font-bold mb-4 uppercase tracking-widest" style="color: #CCA03F;">
          {entry.data.date.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })}
        </p>
        <h1 class="text-4xl md:text-5xl font-extrabold text-slate-900 mb-6 leading-tight">
          {entry.data.title}
        </h1>
        <p class="text-lg text-slate-500 italic">By {entry.data.author}</p>
      </header>

      {entry.data.image && (
        <img src={entry.data.image} alt={entry.data.title} class="w-full h-auto rounded-2xl shadow-lg mb-12 border-2 border-slate-100" />
      )}

      <div class="prose prose-lg prose-slate max-w-none prose-headings:text-slate-900 prose-a:font-semibold prose-a:no-underline hover:prose-a:underline [--tw-prose-links:#CCA03F]">
        <Content />
      </div>
    </div>
  </article>

  <!-- CTA (locked: #1A2C46 bg, #FFFFFF text, #CCA03F button) -->
  <section class="py-16 border-t border-white/10" style="background-color: #1A2C46;">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h2 class="text-2xl font-bold mb-6" style="color: #FFFFFF;">Gain Clarity on Your Financial Future</h2>
      <a
        href="/book"
        class="inline-flex items-center justify-center px-8 py-4 text-lg font-bold rounded-md transition-all shadow-lg hover:brightness-110 hover:scale-[1.02]"
        style="background-color: #CCA03F; color: #FFFFFF;"
      >
        Book a Free Strategy Session
      </a>
    </div>
  </section>
</GlobalLayout>
